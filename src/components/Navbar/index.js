import React from 'react';
import './index.scss';

const Navbar = (props) => {

    return (
        <div id={props.id}>
            <span>Navbar component</span>
        </div>
    );
};

export default Navbar;
